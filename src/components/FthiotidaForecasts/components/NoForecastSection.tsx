export default function NoForecastSection() {
    return (
        <div className="my-4 flex h-[260px] w-full items-center justify-center">
            <h3 className="my-2 font-semibold text-primary">
                No clouds in sight — at least not in our data!
            </h3>
        </div>
    );
}